{
  "name": "pagoseguro-portal-transacciones",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r app/* dist/ && echo build-ok",
    "start": "npx serve app"
  }
}
